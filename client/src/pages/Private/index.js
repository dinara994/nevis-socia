import React from 'react';
import Layout from "../../components/Layout";

const Private = () => {
    return (
        <Layout>
            Private
        </Layout>
    );
};

export default Private;